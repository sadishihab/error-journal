{
  "schema": 2,
  "permissions": [
    "tools.invoke",
    "storage.read",
    "storage.write"
  ],
  "required_executas": [
    {
      "tool_id": "bundled:error-journal"
    }
  ],
  "system_prompt_addendum": "When the user pastes an error, traceback, stack trace, or failing log output, call the error-journal diagnose_error tool with the raw text verbatim. When it returns, print the `headline` field first and word-for-word \u2014 it states whether the user has hit this exact problem before and what fixed it last time, which is the reason this app exists. Then give root_cause, then fix_steps as an ordered list without rewording them, then verify_command, and always include the fingerprint. Never substitute your own diagnosis for the tool's.",
  "tags": [
    "debugging",
    "devops",
    "errors",
    "kubernetes",
    "docker",
    "python"
  ],
  "ui": {
    "bundle": {
      "format": "static-spa",
      "entry": "index.html",
      "external_origins": []
    },
    "views": [
      {
        "name": "main",
        "title": "Error Journal",
        "default": true,
        "default_size": {
          "w": 720,
          "h": 620
        },
        "min_size": {
          "w": 380,
          "h": 400
        },
        "max_size": {
          "w": 1400,
          "h": 1100
        },
        "single_instance": true
      }
    ],
    "host_api": {
      "tools": [
        "required:bundled:error-journal"
      ],
      "storage": [
        "get",
        "set"
      ],
      "window": [
        "set_title",
        "ready"
      ]
    },
    "csp_overrides": {},
    "state_merge": "last_writer_wins"
  },
  "dev": {
    "fixtures": [
      "fixtures/*.jsonl"
    ],
    "seed_storage": {},
    "user_id": 1
  },
  "host_capabilities": [
    "aps.kv",
    "llm.sample"
  ]
}
